import FirebaseContext, { withFirebase, withFirebasePage } from './FirebaseContext';
import Firebase from './Firebase';

export default Firebase;

export { FirebaseContext, withFirebase, withFirebasePage };