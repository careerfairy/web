import React from 'react';
import {makeStyles} from "@material-ui/core/styles";

const useStyles = makeStyles(theme => ({}));

const TeamBiosSection = ({}) => {

    const classes = useStyles()

    return (
        <div>
            Team Bios
        </div>
    );
};

export default TeamBiosSection;
