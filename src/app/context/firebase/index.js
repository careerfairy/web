import FirebaseContext, { withFirebase, withFirebasePage,useFirebase } from './FirebaseContext';
import Firebase from 'data/firebase/Firebase';

export default Firebase;

export { FirebaseContext, withFirebase, withFirebasePage,useFirebase };