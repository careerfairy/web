# Back-fill Live stream tags

Will fetch all sparks, and for each spark will get the video duration and update the video object accordingly.

## Run

```sh
npm run script -w @careerfairy/firebase-scripts scriptPath=./migrations/sparks/backfillSparksVideoDuration
```
