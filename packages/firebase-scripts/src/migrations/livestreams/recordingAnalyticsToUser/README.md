# Populate recordingStatsUser with old recording analytics

Populate the `recordingStatsUser` subcollection with old recording analytics.

## Run

```sh
npm run script -w @careerfairy/firebase-scripts -- scriptPath=./migrations/livestreams/recordingAnalyticsToUser
```
