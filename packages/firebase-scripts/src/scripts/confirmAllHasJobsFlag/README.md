# Confirm all has jobs flag

## Usage

To run the script, use the following command:

```bash
npm run script -w @careerfairy/firebase-scripts -- scriptPath=./scripts/confirmAllHasJobsFlag
```

## What it does

This script goes to all the live streams that have `hasJobs` flag as true and validates if the flag is correct. If the group still has that jobs as valid.
