{
   "name": "@careerfairy/shared-lib",
   "private": true,
   "version": "0.0.0",
   "main": "./dist/index.js",
   "scripts": {
      "build": "npm run clean && npm run copy-files && tsc",
      "dev": "npm run clean && npm run copy-files && tsc --watch",
      "copy-files": "cpx \"src/**/*.{json}\" dist/",
      "clean": "rimraf dist/"
   },
   "dependencies": {
      "object-hash": "^3.0.0"
   },
   "peerDependencies": {
      "axios": "^0.27.2",
      "firebase": "^9.9.0"
   },
   "devDependencies": {
      "cpx": "^1.5.0",
      "rimraf": "^3.0.2",
      "typescript": "^4.5.4"
   }
}
