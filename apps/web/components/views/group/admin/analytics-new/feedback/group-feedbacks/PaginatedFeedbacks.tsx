import { FC } from "react"
import { Grid } from "@mui/material"

type Props = {}

const PaginatedFeedbacks: FC<Props> = (props) => {
   return (
      <Grid container spacing={3}>
         PaginatedFeedbacks
      </Grid>
   )
}

export default PaginatedFeedbacks
