import { Box } from "@mui/material"
import { sxStyles } from "types/commonTypes"

const styles = sxStyles({ root: {} })

export const SpotlightPDF = () => {
   return <Box sx={styles.root}>SpotlightPDF</Box>
}
