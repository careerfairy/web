import { Box } from "@mui/material"
import { styled } from "@mui/material"

const SparkAspectRatioBox = styled(Box)(() => ({
   width: "100%",
   aspectRatio: "9 / 16",
   overflow: "hidden",
}))

export default SparkAspectRatioBox
