import { create } from "@storybook/theming"

export const theme = create({
   base: "light",
   brandTitle: `Website - CareerFairy - Streaming`,
   brandUrl: "https://github.com/mkosir/turborepo-boilerplate",
})
