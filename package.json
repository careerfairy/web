{
  "private": true,
  "name": "careerfairy-root",
  "scripts": {
    "start:emulators": "cross-env FIREBASE_AUTH_EMULATOR_HOST=localhost:9099 FIRESTORE_EMULATOR_HOST=localhost:8080 NEXT_PUBLIC_FIREBASE_EMULATORS=true npm run start:emulators:helper",
    "start:emulators:helper": "firebase emulators:exec --ui --only auth,firestore,functions \"cd ./app && npm run dev\"",
    "start:emulators:helper:build": "firebase emulators:exec --ui --only auth,firestore,functions \"cd ./app && npm run start\"",
    "prepare:manual": "if [[ $NODE_ENVIRONMENT != \"production\" ]]; then husky install; fi",
    "build:app": "cd app && npm run build",
    "build:functions": "cd functions && npm run build",
    "start:app:build:with:emulators": "cross-env FIREBASE_AUTH_EMULATOR_HOST=localhost:9099 FIRESTORE_EMULATOR_HOST=localhost:8080 NEXT_PUBLIC_FIREBASE_EMULATORS=true npm run start:emulators:helper:build",
    "install:app": "cd app && npm install",
    "install:functions": "cd functions && npm install",
    "postinstall": "cross-env run-p install:*",
    "test:e2e": "cd app && npm run test:e2e",
    "test": "cd app && npm run test"
  },
  "devDependencies": {
    "@playwright/test": "^1.20.0",
    "cross-env": "^7.0.3",
    "firebase-tools": "^10.2.2",
    "husky": "^7.0.4",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.5.1"
  },
  "lint-staged": {
    "*.js": "prettier --write --ignore-unknown"
  }
}
